// Self-explanatory, fill in your info.
// You may have to create a database ("rhymedoctor") from the command line first.
// e.g. mysql -u root -p
//      (type in password for "root")
//      create database rhymedoctor;
//      (exit MySQL command-line, probably CTRL-D)

module.exports = {
  user: 'root',
  pass: null,
};
